import { StarsDifficultyLevelPipe } from './stars-difficulty-level.pipe';

describe('StarsDifficultyLevelPipe', () => {
  it('create an instance', () => {
    const pipe = new StarsDifficultyLevelPipe();
    expect(pipe).toBeTruthy();
  });
});
